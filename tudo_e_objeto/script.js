import react from 'react'
import axios from 'axios'
/* class Menu {
    constructor(menu) {
        this.menuElement = document.querySelector(menu)
    }
    addActiveElement() {
        this.menuElement.addActiveElement("clik", event)
        event.target.classlist.add("active")
    }
}

const menu = new Menu(".principal")

console.log(menu)
 */
function myButton() {
    return (
        <button>
            Click!
        </button>
    )
}